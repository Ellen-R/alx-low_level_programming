#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point of progam
 * Return: 0 on success
 */

int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	{
		printf("%d", number);
	}
	printf("\n");

	return (0);
}
